ERROR_MANUALS = {
    "missing_input": {
        "msg": "missed argument: input file path was not given\n",
        "hint": "Use the -i flag followed by the input file path, e.g., -i file.txt"
    },
    "invalid_void_flag": {
        "msg": "invalid value for void flag {flag}\n",
        "hint": "Void flags like {flag} should be passed without a value (just the flag itself)."
    },
    "missing_value_flag": {
        "msg": "missing or invalid value for flag {flag}\n",
        "hint": "Make sure to provide a value after the flag, e.g., -o output.txt"
    }
}
